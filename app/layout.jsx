export const metadata = {
  title: "Audit Mailchimp (lecture seule)"
};

export default function RootLayout({ children }) {
  return (
    <html lang="fr">
      <body>{children}</body>
    </html>
  );
}

